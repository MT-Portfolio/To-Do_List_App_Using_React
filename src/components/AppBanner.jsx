import React, { useState, useEffect } from "react";
import ReactDOM from "react-dom";

function AppBanner() {
  const Styles = {
    paddingTop: "1rem",
    textAlign: "center",
  };
  return <h1 style={Styles}>To Do App</h1>;
}

export default AppBanner;
